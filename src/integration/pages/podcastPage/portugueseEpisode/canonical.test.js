/**
 * @service portuguese
 * @pathname /portuguese/podcasts/p07r3r3t/p083x9gr
 */

import runCrossPlatformTests from '../crossPlatformTests';
import runCanonicalTests from '../canonicalTests';

describe('Canonical', () => {
  describe(pageType, () => {
    runCrossPlatformTests(service);
    runCanonicalTests();
  });
});
