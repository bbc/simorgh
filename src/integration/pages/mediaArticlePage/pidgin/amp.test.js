/**
 * @service pidgin
 * @pathname /pidgin/articles/cvpde7nqj92o
 */

import runAmpTests from '../ampTests';

describe('AMP', () => {
  describe(pageType, () => {
    runAmpTests(service);
  });
});
