/**
 * @service scotland
 * @pathname /scotland/articles/czwj5l0n210o
 */

import runAmpTests from '../ampTests';

describe('AMP', () => {
  describe(pageType, () => {
    runAmpTests(service);
  });
});
