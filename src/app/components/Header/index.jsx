import React from 'react';
import BrandContainer from '../../containers/BrandContainer';

const Header = () => (
  <header role="banner">
    <BrandContainer />
  </header>
);

export default Header;
