import { BrandSVG } from '../../../models/types/theming';

// Chameleon logos have an additional margin on their viewbox
const buildLogo = (BrandSVG: BrandSVG) => BrandSVG;

export default buildLogo;
