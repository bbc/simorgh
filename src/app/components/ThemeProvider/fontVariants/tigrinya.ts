import { NOTO_SANS_ETHIOPIC } from '../fontFamilies';

const tigrinya = {
  sans: {
    regular: {
      fontFamily: NOTO_SANS_ETHIOPIC,
      fontWeight: 400,
      fontStyle: 'normal',
    },
    bold: {
      fontFamily: NOTO_SANS_ETHIOPIC,
      fontWeight: 700,
      fontStyle: 'normal',
    },
  },
};

export default tigrinya;
