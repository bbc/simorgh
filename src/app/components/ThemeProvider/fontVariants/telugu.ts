import { NOTO_SANS_TELUGU } from '../fontFamilies';

const telugu = {
  sans: {
    regular: {
      fontFamily: NOTO_SANS_TELUGU,
      fontWeight: 400,
      fontStyle: 'normal',
    },
    bold: {
      fontFamily: NOTO_SANS_TELUGU,
      fontWeight: 700,
      fontStyle: 'normal',
    },
  },
};

export default telugu;
