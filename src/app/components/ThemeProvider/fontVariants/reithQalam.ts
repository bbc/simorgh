import { REITH_QALAM } from '../fontFamilies';

const reithQalam = {
  sans: {
    regular: {
      fontFamily: REITH_QALAM,
      fontWeight: 400,
      fontStyle: 'normal',
    },
    bold: {
      fontFamily: REITH_QALAM,
      fontWeight: 700,
      fontStyle: 'normal',
    },
  },
};

export default reithQalam;
