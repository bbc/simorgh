import { css } from '@emotion/react';

export default {
  example3dScene: () =>
    css({
      width: 500,
      height: 500,
    }),
  canvasContainer: () =>
    css({
      width: '100%',
      height: '100%',
    }),
};
