## Description

This component renders the estimated read time for an article, by using the `readTime` parameter passed into it.

If no `readTime` is supplied nothing is rendered.

If no translations exist for a service, then the Read Time is not rendered. This is intentional because we have only collected translations for services which we believe the default calculation done by Ares is suitable for.
