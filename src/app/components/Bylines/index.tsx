import React from 'react';
import PropTypes from 'prop-types';

type Props = {
  author: string;
  jobRole: string;
  className: string;
};

const Byline = ({ author, jobRole, className }: Props) => {
  return <div></div>;
};

Byline.propTypes = {};

export default Byline;
