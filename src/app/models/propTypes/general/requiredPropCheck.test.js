import requiredPropCheck from './requiredPropCheck';

describe('requiredPropCheck', () => {
  it('should return an array of missing required props', () => {});

  it('should return an empty array of no missing required props', () => {});
});
