const toggles = {
  test: {
    mpulse: {
      enabled: false,
    },
    mediaPlayer: {
      enabled: true,
    },
    chartbeatAnalytics: {
      enabled: true,
    },
  },
  live: {
    mpulse: {
      enabled: false,
    },
    mediaPlayer: {
      enabled: false,
    },
    chartbeatAnalytics: {
      enabled: false,
    },
  },
};

export default toggles;
