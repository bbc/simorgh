const toggles = {
  test: {
    mpulse: {
      enabled: false,
    },
    mediaPlayer: {
      enabled: true,
    },
    chartbeatAnalytics: {
      enabled: true,
    },
  },
  live: {
    mpulse: {
      enabled: false,
    },
    mediaPlayer: {
      enabled: false,
    },
    chartbeatAnalytics: {
      enabled: true,
    },
  },
};

export default toggles;
