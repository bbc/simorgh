const toggles = {
  test: {
    mpulse: {
      enabled: false,
    },
    mediaPlayer: {
      enabled: true,
    },
  },
  live: {
    mpulse: {
      enabled: false,
    },
    mediaPlayer: {
      enabled: false,
    },
  },
};

export default toggles;
