import React from 'react';

// Global styles are now handled by Tailwind CSS and our global CSS files
// This component is kept for compatibility but no longer renders global styles
const GlobalStyles = () => null;

export default GlobalStyles;
