import React from 'react';

const ArticleTimestamp = ({
  // eslint-disable-next-line no-unused-vars
  firstPublished,
  // eslint-disable-next-line no-unused-vars
  lastPublished,
  // eslint-disable-next-line no-unused-vars
  popOut = true,
  // eslint-disable-next-line no-unused-vars
  minutesTolerance = 0,
  // eslint-disable-next-line no-unused-vars
  className = '',
  // eslint-disable-next-line no-unused-vars
  showReadTimeBelowTimestamp = false,
}) => {
  return <p>Article Timestamp</p>;
};

export default ArticleTimestamp;
