/* eslint-disable no-unused-vars */
// eslint-disable-next-line import/prefer-default-export
export const getBrandedImage = (image, service) => {
  // const imageUrl = image
  //   .split('/')
  //   .splice(3)
  //   .join('/');
  // return `http://ichef.bbci.co.uk/news/1024/branded_${service}/${imageUrl}`;

  // Placeholder function to get branded image
  return image;
};
