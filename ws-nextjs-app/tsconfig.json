{
  "extends": "../tsconfig.json",
  "compilerOptions": {
    "declaration": false, // Look into removing this when we can combine package.json files between both apps
    "jsx": "preserve"
  },
  "include": ["next-env.d.ts", "**/*.ts", "**/*.tsx", "next.config.js"],
  "exclude": ["node_modules", "build", "src/app/legacy/**/*"]
}
