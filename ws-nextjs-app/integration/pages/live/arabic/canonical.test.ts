/**
 * @service arabic
 * @pathname /arabic/live/67574192
 */

import { runHeaderTests } from '../../../common';

describe('Canonical', () => {
  describe(pageType, () => {
    runHeaderTests(service);
  });
});
