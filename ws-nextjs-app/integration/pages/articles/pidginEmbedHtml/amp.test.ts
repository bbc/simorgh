/**
 * @service pidgin
 * @pathname /pidgin/articles/c39rjygpmv1o
 */

import { runAmpEmbedHtmlTests } from '../../../common';

describe('AMP', () => {
  describe(pageType, () => {
    runAmpEmbedHtmlTests();
  });
});
