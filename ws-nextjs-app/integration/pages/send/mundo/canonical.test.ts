/**
 * @service mundo
 * @pathname /mundo/send/u50853489?renderer_env=live
 */

import runCanonicalTest from '../canonicalTests';

describe('Canonical', () => {
  describe(pageType, () => {
    runCanonicalTest();
  });
});
