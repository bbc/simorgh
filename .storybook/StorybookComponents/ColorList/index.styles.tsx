import { css, Theme } from '@emotion/react';

// Border uses grey-5
const styles = {
  colorGrid: {
    display: 'grid',
    gap: '1rem',
    gridTemplateColumns: 'repeat(3, 1fr)',
    listStyle: 'none',
    padding: 0,
  },
};

export default styles;
