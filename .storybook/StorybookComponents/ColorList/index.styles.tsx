import { css, Theme } from '@emotion/react';

const styles = {
  colorGrid: {
    display: 'grid',
    gap: '1rem',
    gridTemplateColumns: 'repeat(3, 1fr)',
    listStyle: 'none',
    padding: 0,
  },
};

export default styles;
