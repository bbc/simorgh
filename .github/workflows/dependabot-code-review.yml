name: Dependabot Code Review
on:
  pull_request:
    # types:
    #   - opened
    branches:
      - '**'

jobs:
  run:
    name: Request Code Review
    runs-on: ubuntu-latest
    steps:
      - name: Post Slack Message
        uses: slackapi/slack-github-action@v2.1.0
        with:
          webhook: ${{ secrets.SLACK_WEBHOOK_URL }}
          webhook-type: incoming-webhook
          payload: |
            "text": "Please review this Pull Request: *<${{ github.event.pull_request.html_url }}|${{github.event.pull_request.title}}>*"
            # "blocks": [
            #   {
            #     "type": "section",
            #     "text": {
            #       "type": "mrkdwn",
            #       "text": "*<${{ github.event.pull_request.html_url }}|${{github.event.pull_request.title}}>*"
            #     }
            #   }
            # ]


            		          # {
              #   "type": "section",
              #   "fields": [
              #     {
              #       "type": "mrkdwn",
              #       "text": "*Repo:* ${{ github.event.pull_request.head.repo.name }}"
              #     },
              #     {
              #       "type": "mrkdwn",
              #       "text": "*Author:* ${{ github.event.pull_request.user.login }}"
              #     },
              #     {
              #       "type": "mrkdwn",
              #       "text": "*Files Changed:* ${{ github.event.pull_request.changed_files }}"
              #     }
              #   ]
          # }
          # {
          #   "type": "actions",
          #   "elements": [
          #     {
          #       "type": "button",
          #       "text": {
          #         "type": "plain_text",
          #         "emoji": true,
          #         "text": "Approve"
          #       },
          #       "style": "primary",
          #       "value": "click_me_123"
          #     },
          #     {
          #       "type": "button",
          #       "text": {
          #         "type": "plain_text",
          #         "emoji": true,
          #         "text": "Deny"
          #       },
          #       "style": "danger",
          #       "value": "click_me_123"
          #     }
          #   ]
          # }
