name: Simorgh CI - Licences, Dependencies, Lint, Chromatic UI, NPM Audit
on:
  create:
    branches:
      - '**'
  pull_request:
    branches:
      - '**'
  push:
    branches:
      - latest
jobs:
  build:
    runs-on: ubuntu-latest
    permissions:
      statuses: write
    strategy:
      matrix:
        node-version: [16.x]
    env:
      CI: true

    steps:
      - uses: actions/checkout@v3
        with:
          fetch-depth: '0'

      - name: Use Node.js ${{ matrix.node-version }}
        uses: actions/setup-node@v3
        with:
          node-version: ${{ matrix.node-version }}

      - name: Cache Node Modules
        id: cache
        uses: actions/cache@v3
        with:
          path: node_modules
          key: node-modules-${{ hashFiles('yarn.lock') }}

      - name: Install Node Modules
        if: steps.cache.outputs.cache-hit != 'true'
        run: ./scripts/installNodeModules.sh

      - name: Get Branch Name for Storybook Permalink
        uses: actions/github-script@v6
        id: get-storybook-branch-name
        with:
          script: |
            core.setOutput('STORYBOOK_BRANCH_NAME', '${{ github.event.pull_request.head.ref }}'.toLowerCase().replaceAll(/[/.]/g, '-').trim('-').substring(0, 37));
      
      - name: Publish Storybook Branch Permalink
        if: github.event.pull_request.head.repo.full_name == 'bbc/simorgh' # Only run when not on a fork
        run: |
          export STORYBOOK_PERMALINK="https://${{ steps.get-storybook-branch-name.outputs.STORYBOOK_BRANCH_NAME }}--5d28eb3fe163f6002046d6fa.chromatic.com"
          echo "Storybook Permalink is: ${{ STORYBOOK_PERMALINK }}"
          curl \
          -X POST \
          -H "Authorization: Bearer ${{ secrets.GITHUB_TOKEN }}" \
          -H "Accept: application/vnd.github+json" \
          -H "X-GitHub-Api-Version: 2022-11-28" \
          https://api.github.com/repos/${{ github.repository }}/statuses/${{ github.event.pull_request.head.sha }} \
          -d '{
            "state": "success", 
            "context": "Storybook Permalink", 
            "description": "View the latest changes on Storybook for this PR",
            "target_url": "${{ STORYBOOK_PERMALINK }}"
          }' \
          --fail

      - name: Chromatic UI Tests
        uses: chromaui/action@v1
        if: github.ref != 'refs/heads/latest' && github.event.pull_request.head.repo.full_name == 'bbc/simorgh' # Only run when not on latest or not a fork.
        with:
          token: ${{ secrets.SIMORGH_DEV_STORYBOOK_RELEASE }}
          projectToken: ${{ secrets.CHROMATIC_APP_CODE }}
          buildScriptName: 'build:storybook'
          exitOnceUploaded: true

      - name: Chromatic UI Tests - Latest
        uses: chromaui/action@v1
        if: github.ref == 'refs/heads/latest' # Only run on latest branch
        with:
          token: ${{ secrets.SIMORGH_DEV_STORYBOOK_RELEASE }}
          projectToken: ${{ secrets.CHROMATIC_APP_CODE }}
          buildScriptName: 'build:storybook'
          exitOnceUploaded: true
          autoAcceptChanges: true # Auto accept changes to accept a new baseline when merging to latest

      - name: Duplicate Dependency Checker
        run: yarn test:dependencies

      - name: Linting
        run: yarn test:lint

      - name: NPM Audit
        run: npx audit-ci --config audit-ci.json
